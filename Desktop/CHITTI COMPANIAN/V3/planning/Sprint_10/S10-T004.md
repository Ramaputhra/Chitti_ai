# Task ID: S10-T004

**Title:** Mail & Calendar Capabilities
**Owner:** Productivity Engineer
**Priority:** High

## Description
Implement `MailCapability` and `CalendarCapability` backed by respective Provider interfaces (`IMailProvider`, `ICalendarProvider`). Adopt a "Query" tool pattern (`MailQuery`, `CalendarQuery`) rather than rigid endpoints, which outputs lists of `EmailArtifact` and `CalendarArtifact`.

## Definition of Done
- [x] Code implemented
- [x] Merged into develop
