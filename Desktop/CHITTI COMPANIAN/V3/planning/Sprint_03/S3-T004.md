# Task ID: S3-T004

**Title:** Speaker Manager
**Owner:** Audio Firmware Engineer
**Priority:** High

## Description
Implement `ISpeakerManager` (inherits `IService`). Wraps `sounddevice.play()` to execute blocking calls inside a daemon thread. Emits `Voice.PlaybackStarted` and `Voice.PlaybackFinished`.

## Definition of Done
- [x] Code implemented
- [x] Merged into develop
