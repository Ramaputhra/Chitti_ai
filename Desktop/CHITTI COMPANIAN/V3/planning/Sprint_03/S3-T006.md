# Task ID: S3-T006

**Title:** Audio Pipeline
**Owner:** Audio Firmware Engineer
**Priority:** Critical

## Description
Implement `IAudioPipeline`. Connects to the `Voice.AudioFrame` event, runs frames through the VAD Strategy, manages `SpeechStarted` and `SpeechEnded` events, and emits the final `Voice.AudioReady` containing the complete `AudioPacket`. Does NOT convert to text.

## Definition of Done
- [x] Code implemented
- [x] Merged into develop
