# Task ID: CP2-T004

**Title:** Screen Analyzer & Runtime Manager
**Owner:** Perception Engineer
**Priority:** High

## Description
Implement the `ScreenAnalyzer` class which acts as the aggregation point, passing the empty `ScreenModel` sequentially to all registered `IScreenProvider` instances to be enriched. Implement the `ScreenRuntimeManager` to orchestrate the entire tick: diffing the frame, querying the focused app, triggering the Analyzer, breaking the resulting `ScreenModel` into `ScreenArtifacts`, and publishing them to the `PerceptionBus`.

## Definition of Done
- [x] Code implemented
- [x] Merged into develop
