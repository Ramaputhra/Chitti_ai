# Task ID: CP2-T001

**Title:** Screen Models & Artifacts
**Owner:** Perception Engineer
**Priority:** High

## Description
Implement `ScreenModel` and `CursorState`. The `ScreenModel` acts as the mandatory domain aggregation layer (per Engineering Rule #14) where OCR, UI detection, and window layouts merge before being split into `ScreenArtifacts`. Define the `ScreenArtifact` hierarchy (`ScreenshotArtifact`, `WindowArtifact`, `UIElementArtifact`), which inherits from `PerceptionArtifact`.

## Definition of Done
- [x] Code implemented
- [x] Merged into develop
