# Task ID: CP2-T003

**Title:** Core Screen Providers
**Owner:** Perception Engineer
**Priority:** High

## Description
Implement `IScreenProvider` where the `analyze()` method takes the raw frame and the `ScreenModel` to enrich it in place. Implement `ScreenshotProvider`, `CursorTrackingProvider` (which maps rich cursor state), and `UIDetectionProvider`. Ensure no provider bypasses the `ScreenModel` to generate individual artifacts.

## Definition of Done
- [x] Code implemented
- [x] Merged into develop
