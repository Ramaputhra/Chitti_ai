# Task ID: CP3-T004

**Title:** Robot Analyzer & Runtime Manager
**Owner:** Hardware API Engineer
**Priority:** High

## Description
Implement the `RobotAnalyzer` to safely construct the `RobotModel` by passing it through all registered hardware providers. Implement the `RobotRuntimeManager` to orchestrate this tick and subsequently shatter the `RobotModel` into discrete `RobotArtifacts` (Touch, Sensor, Telemetry), publishing them safely onto the `PerceptionBus`.

## Definition of Done
- [x] Code implemented
- [x] Merged into develop
