# Task ID: CP1-T003

**Title:** Vision Runtime & Provider Abstraction
**Owner:** Vision Engineer
**Priority:** High

## Description
Implement `IVisionProvider` with the standard CHITTI lifecycle methods (`initialize`, `shutdown`, `health_check`, `detect`, `describe`, `supports`, `benchmark`). Implement the `VisionRuntimeManager` to orchestrate preprocessing, provider execution, and finally publishing the resulting `VisionArtifacts` as `AttentionEvents` to the `PerceptionBus`.

## Definition of Done
- [x] Code implemented
- [x] Merged into develop
